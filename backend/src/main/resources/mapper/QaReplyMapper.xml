<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teaching.mapper.QaReplyMapper">

    <select id="selectRepliesByQuestionId" resultType="com.teaching.entity.QaReply">
        SELECT r.*, u.real_name AS user_name, u.avatar AS user_avatar
        FROM qa_reply r
        LEFT JOIN sys_user u ON r.user_id = u.id
        WHERE r.question_id = #{questionId}
        ORDER BY r.create_time ASC
    </select>

</mapper>
