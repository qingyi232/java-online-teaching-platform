<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teaching.mapper.ResourceCommentMapper">

    <select id="selectCommentsByResourceId" resultType="com.teaching.entity.ResourceComment">
        SELECT c.*, u.real_name AS user_name, u.avatar AS user_avatar
        FROM resource_comment c
        LEFT JOIN sys_user u ON c.user_id = u.id
        WHERE c.resource_id = #{resourceId}
        ORDER BY c.create_time ASC
    </select>

</mapper>
